#ifndef _delay_h_                 // Include guard start to prevent multiple inclusion
#define _delay_h_                 // Define macro for include guard

void delay_us(unsigned int i);   // Function prototype for microsecond delay
void delay_ms(unsigned int i);   // Function prototype for millisecond delay
void delay_s(unsigned int i);    // Function prototype for second delay

#endif                           // End of include guard
